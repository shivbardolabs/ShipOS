import SwiftUI

/// Mail management view (Phase 3 — placeholder).
struct MailListView: View {
    var body: some View {
        SOEmptyState(
            icon: "envelope.badge",
            title: "Mail",
            message: "Mail management is coming in Phase 3."
        )
    }
}
